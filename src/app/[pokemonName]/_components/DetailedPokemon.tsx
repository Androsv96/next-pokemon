//use client

import { PokemonDetails } from "@/app/interfaces";

interface Props {
  pokemonData: PokemonDetails;
}

const DetailedPokemon = ({ pokemonData }: Props) => {
  return (
    <div>
      <h1>hola</h1>
    </div>
  );
};

export default DetailedPokemon;
